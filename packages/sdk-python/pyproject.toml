[project]
name = "akep-sdk"
version = "0.1.0"
description = "Typed, policy-aware client for AKEP knowledge services"
requires-python = ">=3.11"

[build-system]
requires = ["hatchling>=1.27,<2"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["akep_sdk"]
